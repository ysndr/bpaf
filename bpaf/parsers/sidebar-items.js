window.SIDEBAR_ITEMS = {"struct":[["ParseBox","Create boxed parser"],["ParseMany","Apply inner parser several times and collect results into `Vec`, created with `many`, implements `catch`."],["ParseOptional","Apply inner parser, return a value in `Some` if items requested by it are all present, restore and return `None` if any are missing. Created with `optional`. Implements `catch`"],["ParseSome","Apply inner parser several times and collect results into `Vec`, created with `some`, requires for at least one item to be available to succeed. Implements `catch`"]]};